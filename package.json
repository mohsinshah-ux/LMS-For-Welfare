{
  "name": "lms-for-welfare",
  "private": true,
  "version": "1.0.0",
  "description": "Islamic LMS — Vercel + Supabase monorepo",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend"
  }
}
