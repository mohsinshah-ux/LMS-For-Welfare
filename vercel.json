{
  "framework": null,
  "installCommand": "cd frontend && rm -f package-lock.json && npm install --no-audit --no-fund --legacy-peer-deps",
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/.next"
}
