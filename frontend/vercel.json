{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "rm -rf node_modules .next && npm install --no-audit --no-fund --legacy-peer-deps",
  "buildCommand": "node ./node_modules/next/dist/bin/next build"
}
