{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "rm -rf node_modules .next package-lock.json && npm install --no-audit --no-fund --legacy-peer-deps",
  "buildCommand": "npm run build"
}
